{
  "name": "mcp-lab-workshop",
  "version": "1.0.0",
  "description": "MCP Lab Workshop - Learn MCP Servers with Cloudflare Workers",
  "private": true,
  "scripts": {
    "build": "echo 'Build complete'",
    "deploy": "npx wrangler deploy demo-app/index.js --name mcp-demo-app && npx wrangler deploy mcp-server/index.js --name mcp-server"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "cloudflare": {
    "bindings": {
      "DEMO_STATE": {
        "description": "KV namespace for storing demo application state. This will be automatically provisioned during deployment."
      }
    }
  },
  "keywords": ["mcp", "model-context-protocol", "cloudflare", "workers", "workshop"],
  "author": "Cloudflare",
  "license": "MIT"
}
